---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: infrastacks.platform.example.io
spec:
  group: platform.example.io
  names:
    kind: InfraStack
    listKind: InfraStackList
    plural: infrastacks
    shortNames:
    - istack
    singular: infrastack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.desiredState
      name: Desired
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              approvalPolicy:
                enum:
                - Manual
                - Automatic
                type: string
              backend:
                properties:
                  authRef:
                    properties:
                      serviceAccountName:
                        type: string
                        x-kubernetes-validations:
                        - message: EnvironmentClass backend auth reference is immutable
                          rule: self == oldSelf
                    required:
                    - serviceAccountName
                    type: object
                    x-kubernetes-validations:
                    - message: EnvironmentClass backend auth reference is immutable
                      rule: self.serviceAccountName == oldSelf.serviceAccountName
                  configRef:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                    x-kubernetes-validations:
                    - message: EnvironmentClass backend config reference is immutable
                      rule: self.name == oldSelf.name
                  lockTimeout:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass backend is immutable
                      rule: duration(self) == duration(oldSelf)
                  type:
                    enum:
                    - s3
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass backend is immutable
                      rule: self == oldSelf
                required:
                - authRef
                - configRef
                - type
                type: object
              capacity:
                properties:
                  nodeCount:
                    format: int32
                    type: integer
                type: object
              concurrencyGroup:
                type: string
              desiredState:
                enum:
                - Present
                - Destroy
                type: string
              executor:
                properties:
                  executionTimeout:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass executor is immutable
                      rule: duration(self) == duration(oldSelf)
                  image:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass executor is immutable
                      rule: self == oldSelf
                  parallelism:
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: EnvironmentClass executor is immutable
                      rule: self == oldSelf
                  runtimeArchitecture:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass executor is immutable
                      rule: self == oldSelf
                  runtimeOS:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass executor is immutable
                      rule: self == oldSelf
                  terraformVersion:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass executor is immutable
                      rule: self == oldSelf
                  workDir:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass executor is immutable
                      rule: self == oldSelf
                required:
                - executionTimeout
                - image
                - terraformVersion
                - workDir
                type: object
              infrastructureExecutionIdentity:
                properties:
                  accountID:
                    type: string
                  mode:
                    type: string
                  provider:
                    type: string
                  region:
                    type: string
                  roleARN:
                    type: string
                  sessionProfile:
                    type: string
                required:
                - accountID
                - mode
                - provider
                - region
                type: object
              maxConcurrentApplies:
                format: int32
                type: integer
              maxConcurrentPlans:
                format: int32
                type: integer
              mutationFence:
                type: boolean
              ownerEnvironmentUID:
                type: string
                x-kubernetes-validations:
                - message: InfraStack ownerEnvironmentUID is immutable
                  rule: self == oldSelf
              runnerServiceAccountName:
                type: string
              runtimeMutationAllowed:
                type: boolean
              runtimeTargetIdentity:
                properties:
                  accountID:
                    type: string
                  clusterARN:
                    type: string
                  clusterName:
                    type: string
                  incarnationID:
                    type: string
                  provider:
                    type: string
                  region:
                    type: string
                required:
                - accountID
                - incarnationID
                - provider
                - region
                type: object
              source:
                properties:
                  path:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass source is immutable
                      rule: self == oldSelf
                  revision:
                    pattern: ^[0-9a-fA-F]{40,64}$
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass source is immutable
                      rule: self == oldSelf
                  url:
                    type: string
                    x-kubernetes-validations:
                    - message: EnvironmentClass source is immutable
                      rule: self == oldSelf
                required:
                - path
                - revision
                - url
                type: object
              targetConnectionProfile:
                properties:
                  authMode:
                    type: string
                  caCertificateData:
                    type: string
                  caCertificateDigest:
                    type: string
                  endpoint:
                    description: |-
                      Endpoint is optional on the desired AWS-native profile and is populated
                      by trusted target discovery before runtime mutation is enabled.
                    type: string
                  kubeContext:
                    type: string
                  networkRouteProfile:
                    type: string
                  roleARN:
                    type: string
                required:
                - authMode
                type: object
              variables:
                properties:
                  secretRefs:
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  secretVariables:
                    items:
                      properties:
                        secretKeyRef:
                          description: SecretKeySelector selects a key of a Secret.
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        variable:
                          type: string
                      required:
                      - secretKeyRef
                      - variable
                      type: object
                    type: array
                type: object
              workspace:
                type: string
            required:
            - backend
            - executor
            - source
            - workspace
            type: object
          status:
            properties:
              cleanupEvidenceDigest:
                type: string
              cleanupEvidenceRef:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              convergenceEvidenceDigest:
                type: string
              convergenceEvidenceRef:
                type: string
              discoveredRuntimeTargetIdentity:
                properties:
                  accountID:
                    type: string
                  clusterARN:
                    type: string
                  clusterName:
                    type: string
                  incarnationID:
                    type: string
                  provider:
                    type: string
                  region:
                    type: string
                required:
                - accountID
                - incarnationID
                - provider
                - region
                type: object
              discoveredTargetConnectionProfile:
                properties:
                  authMode:
                    type: string
                  caCertificateData:
                    type: string
                  caCertificateDigest:
                    type: string
                  endpoint:
                    description: |-
                      Endpoint is optional on the desired AWS-native profile and is populated
                      by trusted target discovery before runtime mutation is enabled.
                    type: string
                  kubeContext:
                    type: string
                  networkRouteProfile:
                    type: string
                  roleARN:
                    type: string
                required:
                - authMode
                type: object
              driftReportDigest:
                type: string
              driftReportRef:
                type: string
              lastAppliedRunRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              lastAppliedSourceBundleDigest:
                type: string
              lastAppliedSourceBundleRef:
                type: string
              lastConverged:
                description: |-
                  LastConvergedSourceClosure is the immutable evidence required to safely
                  reconstruct a later Destroy. It is written for both Apply and NoChange.
                properties:
                  backendSnapshotDigest:
                    type: string
                  backendSnapshotRef:
                    type: string
                  effectivePlanInputDigest:
                    type: string
                  executionPlatformIdentityDigest:
                    type: string
                  generation:
                    format: int64
                    type: integer
                  infrastructureExecutionIdentityDigest:
                    type: string
                  runnerServiceAccountIdentityDigest:
                    type: string
                  sourceClosureDigest:
                    type: string
                  sourceClosureRef:
                    type: string
                  targetDiscoveryDigest:
                    type: string
                  targetDiscoveryRef:
                    type: string
                  terraformRunUID:
                    type: string
                required:
                - backendSnapshotDigest
                - backendSnapshotRef
                - effectivePlanInputDigest
                - executionPlatformIdentityDigest
                - generation
                - infrastructureExecutionIdentityDigest
                - runnerServiceAccountIdentityDigest
                - sourceClosureDigest
                - sourceClosureRef
                - targetDiscoveryDigest
                - targetDiscoveryRef
                - terraformRunUID
                type: object
              latestPlanRunRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              observedGeneration:
                format: int64
                type: integer
              targetDiscoveryDigest:
                type: string
              targetDiscoveryRef:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
